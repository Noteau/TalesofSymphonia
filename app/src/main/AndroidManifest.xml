<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.noteau.talesofsymphonia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cite_elfique"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ecran_accueil">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Codex" />
        <activity android:name=".Credits" />
        <activity android:name=".option_game" />
        <activity android:name=".New_game" />
        <activity android:name=".Continue_game" />
        <activity android:name=".Help" />
        <activity android:name=".Social" />
        <activity android:name=".Inventaire" />
        <activity android:name=".Map" />
        <activity android:name=".Classe_selection" />
    </application>

</manifest>